name: dynamic_routes
description: A super easy-to-use routing library for Flutter that allows you to determine routes and the order of those routes all in one place.
repository: https://github.com/Khongchai/dynamic_routes
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  mockito: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0